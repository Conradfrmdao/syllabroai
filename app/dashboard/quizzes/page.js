import DashboardPlaceholder from "@/components/dashboard/DashboardPlaceholder";

export default function Quizzes() {
  return (
    <DashboardPlaceholder
      badge="Quizzes"
      title="Quiz generation now has a proper home"
      description="This area now matches the upgraded dashboard theme so the quizzes experience can grow into the same AI-first design language without feeling disconnected."
      ctaLabel="Create Source Course"
    />
  );
}
