import DashboardPlaceholder from "@/components/dashboard/DashboardPlaceholder";

export default function Flashcardspage() {
  return (
    <DashboardPlaceholder
      badge="Flashcards"
      title="Flashcards are framed for the new study workflow"
      description="The page now feels aligned with the rest of the product while you keep building the actual flashcard logic later. For now, the UI no longer stops at a single heading."
      ctaLabel="Create Learning Prompt"
    />
  );
}
