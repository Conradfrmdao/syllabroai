import DashboardPlaceholder from "@/components/dashboard/DashboardPlaceholder";

export default function ExamsPage() {
  return (
    <DashboardPlaceholder
      badge="Exams"
      title="Exam prep can now sit inside the same visual system"
      description="This screen has been upgraded from a bare text stub into a styled placeholder so the exams feature feels intentional even before the deeper functionality lands."
      ctaLabel="Start with a Course"
    />
  );
}
