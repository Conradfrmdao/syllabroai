import DashboardPlaceholder from "@/components/dashboard/DashboardPlaceholder";

export default function SettingsPage() {
  return (
    <DashboardPlaceholder
      badge="Settings"
      title="Settings now feels part of the product"
      description="The settings area keeps the same upgraded visual language so future account, billing, or preference controls can drop into a UI that already feels finished."
      ctaLabel="Back to Dashboard"
      ctaHref="/dashboard"
    />
  );
}
